spring:
  application:
    name: config-server
  cloud:
    config:
      discovery:
        enabled: true

---

spring:
  profiles: test
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false