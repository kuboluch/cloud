spring:
  application:
    name: message-client
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        prefix: configuration
        defaultContext: apps
        profileSeparator: '::'

---

spring:
  profiles: test
  cloud:
    consul:
      discovery:
        enabled: false
      config:
        enabled: false